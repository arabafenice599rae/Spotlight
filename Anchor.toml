[toolchain]
anchor_version = "1.0.2"

[features]
resolution = true
skip-lint = false

[programs.localnet]
vetrina = "gCxZar2tTSVKE1amCvMW5BcjMXvbqGRCf52n5P15gM4"

[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "cargo test -p vetrina"
# litesvm suite lives in the detached tests-litesvm workspace; run `anchor build` first.
test-litesvm = "bash -c 'cd tests-litesvm && cargo test -- --nocapture'"

[hooks]
